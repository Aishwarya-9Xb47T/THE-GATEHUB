% Owns ONLY \theory for main teaching content
\theory{title={Core Content},body={
Machine learning is a subset of artificial intelligence where systems learn patterns from data.

Key ideas:
- Training data teaches the model
- Features are inputs; labels are outputs (supervised learning)
- Evaluation measures how well the model generalizes
}}
